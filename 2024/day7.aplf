 (p1 p2)←day7 input
 ⎕FR←1287
 i←{⍎¨':'(≠⊆⊢)⍵}¨⊃⎕NGET input 1
 Find←{
     (r o)←⍵ ⋄ b←≢a←⍺
     all←{a⌷⍨⊂1+⍵}¨(l⍴b)∘⊤¨¯1+⍳b*l←¯1+≢o
     ∨/{(⊃o){⍺>r:0⋄~×≢⍵:r=⍺⋄(⍺(⍎⊃⊃⍵)⊃⌽⊃⍵)∇1↓⍵}↓⍉⍵⍪[.5]1↓o}¨all
 }
 p1←+/('+×'∘Find¨i)/⊃¨i
 F←{⍎∊('I20'⎕FMT⍺)(⍕⍵)}
 p2←+/('+×F'∘Find¨i)/⊃¨i
